# CLAUDE.md

このファイルは、このプロジェクトで開発作業を行う際に守るべきルールをまとめたものです。

## プロジェクト概要

Tsurara Idea Lab：AI画像生成用プロンプトを整理して公開する静的サイト（GitHub Pages公開、ビルドステップなし）。

主要ファイル：
- `index.html` … ページ構造
- `script.js` … 記事データの読み込み・フィルタ・検索・表示ロジック
- `style.css` … デザイン
- `data/articles.json` … 記事データ本体

## 共通方針（最優先）

- 既存の仕様、表示、挙動を優先し、依頼されていない変更は行わない。
- 変更前に、対象ファイル・変更内容・影響範囲を簡潔に説明する。
- 大きな変更、破壊的変更、データ削除、ファイル削除、`main`ブランチへのpushは、必ず事前確認を取ってから行う。
- リファクタリングでは、明示的な依頼がない限り既存の挙動と画面デザインを変えない。
- CSSやUIを整理する際も、見た目を独自判断で変更しない。
- 外部ライブラリ、npmパッケージ、ビルド環境、フレームワークを勝手に追加しない。
- 既存コメントや仕様上必要なコードを、不要と断定して勝手に削除しない。
- 未使用に見えるコードは、削除前に用途と影響を確認する。
- 不明点がある場合は推測で実装せず、質問または複数案を提示する。
- 作業完了後は、変更ファイル・変更内容・確認した動作・未確認事項をまとめて報告する。

## `data/articles.json` を編集する際のルール

- 必ず既存の記事オブジェクトの前後にカンマ・括弧の対応を確認してから保存する（構文崩れによりサイト全体が表示不能になった実績があるため）。
- 追記は既存記事を上書きしない。新規記事は配列の要素として追加するのみとする。
- 新しいIDは「アルファベット3文字＋5桁数字」形式を厳守する（`FAS`/`JOB`/`PNF`/`VIS`/`SSN`/`ORI`/`ITM`）。旧カテゴリ（`EV`/`FA`/`CB`/`AI`など）は新規記事には使わない。
- 日英ペアのキー（`title`/`titleEn`など）は可能な限りセットで入れる。英語が無い場合は日本語がフォールバック表示される仕様のため、省略しても表示は破綻しない。
- 編集後は必ずJSONとして妥当かを検証してから確定する。

## カテゴリ・フィルタ判定のルール

- カテゴリを追加・変更する場合、`script.js`内の `categoryNames` / `matchesFilter()` / `badgeHtml()` / `hasOriginal()`（該当する場合）を必ずセットで確認・更新する。
- 旧カテゴリ・旧ラベルの互換分岐は削除しない（既存記事が一覧から消える原因になるため）。新しい互換を足す場合は既存の分岐パターンに倣う。
- `README.md`のIDカテゴリ体系セクションと矛盾する変更をしない。体系を変える場合は同時にREADMEも更新する。

## 画像運用のルール

- 記事画像は `images/articles/<記事ID>.png` に置くのが既定（`articleImage()`が固定生成）。別名やjpg/webpを使う場合のみ、記事JSONに`image`キーを追加する。
- バナー・ガイド画像などは、README記載の推奨サイズに準拠させる。
- 画像が存在しない場合でも表示が壊れないこと（`optional-image`クラスの挙動）を変更後に確認する。

## UI・JavaScript変更のルール

- 現在の静的HTML/CSS/JavaScript構成を維持する。明示的な承認なしに React、Vue、TypeScript、Vite などへの移行や導入は行わない。
- `imageSearchModal`関連（ボタンが存在せず到達不能になっているコード）は、未使用と判断して直ちに削除せず、削除候補として扱う。削除する場合は事前に対象範囲を説明し、確認を取ってから行う。
- `uiText`（日本語/英語のUI文言）に文言を追加する際は、`ja`/`en`両方に同じキーを必ず追加する。
- ボタンやモーダルなどの機能を削除する場合は、対応するHTML/CSS/JSを一式で確認し、参照だけ消してマークアップが残るような状態を新たに作らない。

## 将来的な整理方針（今すぐ実行するルールではない）

- README.mdの変更履歴（v1〜）が長大化しているため、仕様説明と変更履歴（CHANGELOG.md等）の分離は将来的な整理候補とする。実施する場合は事前に提案し、承認を得てから行う。
- `data/articles.json`のカテゴリ別分割などのデータ構造見直しは、記事数や保守性の状況を見て将来検討する項目とし、現時点では必須としない。

## リリース・変更履歴のルール

- README.mdへの追記は「v番号 + 変更内容」の既存フォーマットを踏襲する。
- `data/articles.json`を含めない差し替えパッケージを作る場合は、READMEにその旨を明記し、本番データを誤って上書きしないようにする。

## 破壊的操作・Git操作の確認

- `main`ブランチへの直接pushがそのままGitHub Pagesに反映されるため、記事データやUIロジックの大きな変更は事前に差分を提示してから確定する。
- 画像やJSONの削除・上書きは、必要性を確認してから行う。
- git commit・pushは、明示的な指示がない限り行わない。
